FROM node:8

# The version to install
ARG version="2.2.0"

# Install cloudflare-cli
RUN yarn global add cloudflare-cli@$version
WORKDIR /workspace

# Image metadata
LABEL name="cloudflare-cli" version=$version maintainer="webmaster@robototes.com"

ENTRYPOINT [ "cfcli" ]
