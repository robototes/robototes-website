FROM node:8
LABEL name="cloudflare-cli" version="2.2.0" maintainer="webmaster@robototes.com"

ENV VERSION="2.2.0"

# Install cloudflare-cli
RUN [ "sh", "-c", "yarn", "global", "add", "cloudflare-cli@$VERSION" ]
WORKDIR /workspace

CMD [ "cfcli" ]
