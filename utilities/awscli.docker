FROM python:3.7.0a2-alpine

# The version to install
ARG version="1.11.178"

# Install awscli
RUN pip install awscli==$version
WORKDIR /workspace

# Image metadata
LABEL name="awscli" version=$version maintainer="webmaster@robototes.com"

ENTRYPOINT [ "aws" ]
