FROM python:3.7.0a2-alpine
LABEL name="awscli" version="1.11.178" maintainer="webmaster@robototes.com"

ENV VERSION='1.11.178'

# Install awscli
RUN [ "pip", "install", "awscli==$VERSION" ]
WORKDIR /workspace

CMD [ "aws" ]
